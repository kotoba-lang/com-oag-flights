{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([oag_flights.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'oag_flights.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
